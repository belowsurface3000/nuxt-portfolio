import { defineStore } from "pinia";

export const useNavigationStore = defineStore("NavigationStore", {
    state: () => {
        return {
            showNav: false
        }
    }
    // actions
    // getters
})